---
phase: 01-core-reconciliation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - stream-chat-android-client/src/test/java/io/getstream/chat/android/client/internal/state/plugin/state/channel/internal/MessageIsLocalOnlyTest.kt
  - stream-chat-android-client/src/test/java/io/getstream/chat/android/client/internal/state/plugin/state/channel/internal/ChannelStateImplPreservationTest.kt
autonomous: true
requirements:
  - PRES-01
  - PRES-04
  - PRES-05

must_haves:
  truths:
    - "Both test files compile and all stubs fail with NotImplementedError or @Disabled annotation — no false greens"
    - "Every test case named in VALIDATION.md Wave 0 requirements has a corresponding stub method"
    - "Running the quick-run command produces test output (not a build error)"
  artifacts:
    - path: "stream-chat-android-client/src/test/java/io/getstream/chat/android/client/internal/state/plugin/state/channel/internal/MessageIsLocalOnlyTest.kt"
      provides: "Stub tests for isLocalOnly() predicate — all SyncStatus values + type cases"
      min_lines: 40
    - path: "stream-chat-android-client/src/test/java/io/getstream/chat/android/client/internal/state/plugin/state/channel/internal/ChannelStateImplPreservationTest.kt"
      provides: "Stub tests for setMessagesPreservingLocalOnly — survival, collision, floor, no-DB cases"
      min_lines: 80
  key_links:
    - from: "ChannelStateImplPreservationTest.kt"
      to: "ChannelStateImplTestBase"
      via: "extends or mirrors constructor pattern"
      pattern: "ChannelStateImplTestBase|class ChannelStateImplPreservationTest"
    - from: "MessageIsLocalOnlyTest.kt"
      to: "Message.isLocalOnly()"
      via: "imports and calls the extension function"
      pattern: "isLocalOnly"
---

<objective>
Create failing test stubs for both new test classes. These stubs must exist before Wave 1 implementation begins so that automated verification commands (`--tests "*.MessageIsLocalOnlyTest"` and `--tests "*.ChannelStateImplPreservationTest"`) resolve to real test classes rather than failing with "no tests found" build errors.

Purpose: Nyquist compliance — every implementation task in Wave 1 and Wave 2 has an automated test command that can run immediately after implementation.
Output: Two test files with @Disabled or `TODO()` stubs for every scenario listed in VALIDATION.md.
</objective>

<execution_context>
@/Users/pvelikov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pvelikov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-reconciliation/01-VALIDATION.md
@.planning/phases/01-core-reconciliation/01-RESEARCH.md

<interfaces>
<!-- Key types the stubs reference. From RESEARCH.md verified source. -->

From stream-chat-android-core/.../models/SyncStatus.kt:
```kotlin
enum class SyncStatus(val status: Int) {
    SYNC_NEEDED(-1),
    IN_PROGRESS(3),
    AWAITING_ATTACHMENTS(4),
    COMPLETED(1),
    FAILED_PERMANENTLY(2),
}
```

From stream-chat-android-core/.../models/MessageType.kt:
```kotlin
object MessageType {
    const val EPHEMERAL = "ephemeral"
    const val ERROR = "error"
    const val REGULAR = "regular"
    const val SYSTEM = "system"
}
```

Test base pattern (from ChannelStateImplTestBase — mirrors constructor):
```kotlin
// Existing base class in same test package
// New test class: mirrors setup; uses mock() for ChannelStateImpl or instantiates directly
// Test fixtures: randomMessage(syncStatus = SyncStatus.FAILED_PERMANENTLY) from stream-chat-android-core testFixtures
```

Existing test setup pattern (from ChannelLogicImplTest.kt lines 59-104):
```kotlin
@ExtendWith(TestCoroutineExtension::class)
internal class SomeTest {
    private val scope = TestScope()
    // mock() from mockito-kotlin
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MessageIsLocalOnlyTest.kt stub</name>
  <files>stream-chat-android-client/src/test/java/io/getstream/chat/android/client/internal/state/plugin/state/channel/internal/MessageIsLocalOnlyTest.kt</files>
  <action>
Create a JUnit 5 test class with one @Test @Disabled stub per case from VALIDATION.md. The class tests `Message.isLocalOnly()` which does not exist yet — stubs use `@Disabled("Wave 1 — implement isLocalOnly() first")` so they compile but do not pass.

Package: `io.getstream.chat.android.client.internal.state.plugin.state.channel.internal`

Required stubs (one @Test @Disabled per line):
1. `isLocalOnly returns true for SyncStatus SYNC_NEEDED`
2. `isLocalOnly returns true for SyncStatus IN_PROGRESS`
3. `isLocalOnly returns true for SyncStatus AWAITING_ATTACHMENTS`
4. `isLocalOnly returns true for SyncStatus FAILED_PERMANENTLY`
5. `isLocalOnly returns true for type ephemeral with COMPLETED syncStatus`
6. `isLocalOnly returns true for type error with COMPLETED syncStatus`
7. `isLocalOnly returns false for SyncStatus COMPLETED with type regular`
8. `isLocalOnly returns false for system message with COMPLETED`

Stub body: `TODO("Implement after Wave 1")` or just `assert(false)` with a comment — either compiles.

Import `SyncStatus`, `MessageType`. Import `randomMessage` from `io.getstream.chat.android.client.Mother` or the existing testFixtures pattern used in this module. Import JUnit 5 annotations (`@Test`, `@Disabled`) from `org.junit.jupiter.api`.

Do NOT call `Message.isLocalOnly()` yet — the extension does not exist. The stub bodies should just mark intent.
  </action>
  <verify>
    <automated>cd /Users/pvelikov/StudioProjects/stream-chat-android && ./gradlew :stream-chat-android-client:compileTestKotlin -x lint 2>&1 | tail -20</automated>
  </verify>
  <done>File exists, compiles without error, contains 8 @Disabled @Test stubs.</done>
</task>

<task type="auto">
  <name>Task 2: Create ChannelStateImplPreservationTest.kt stub</name>
  <files>stream-chat-android-client/src/test/java/io/getstream/chat/android/client/internal/state/plugin/state/channel/internal/ChannelStateImplPreservationTest.kt</files>
  <action>
Create a JUnit 5 test class with one @Test @Disabled stub per case from VALIDATION.md. Tests `ChannelStateImpl.setMessagesPreservingLocalOnly()` which does not exist yet — stubs use `@Disabled("Wave 2 — implement setMessagesPreservingLocalOnly() first")`.

Package: `io.getstream.chat.android.client.internal.state.plugin.state.channel.internal`

Class setup: mirror the setup pattern from `ChannelStateImplTestBase` (read the base class to understand how `ChannelStateImpl` is constructed in tests — use the same constructor parameters or mocking approach). Use `@ExtendWith(TestCoroutineExtension::class)` if the base class uses it.

Required stubs (one @Test @Disabled per line):
1. `failed message survives setMessagesPreservingLocalOnly with non-overlapping incoming`
2. `ephemeral message survives setMessagesPreservingLocalOnly`
3. `AWAITING_ATTACHMENTS message survives setMessagesPreservingLocalOnly`
4. `pending edit SYNC_NEEDED on existing server ID survives setMessagesPreservingLocalOnly`
5. `server COMPLETED version wins when same ID in both incoming and local-only`
6. `below-floor local-only excluded above-floor included`
7. `floor boundary message at exactly floor date is included`
8. `empty incoming page with null floor includes all local-only`
9. `localOnlyFromDb empty no-DB path local-only from state messages value preserved`
10. `localOnlyFromDb non-empty union of state and DB deduped`
11. `COMPLETED messages not re-inserted from state isLocalOnly returns false`
12. `setMessages DB seed does NOT preserve local-only full replace semantics intact`

Import `SyncStatus`, `MessageType`. Import `randomMessage` from the testFixtures. Stub bodies: `TODO("Implement after Wave 2")`.

Do NOT call `setMessagesPreservingLocalOnly` yet — the method does not exist. Stubs mark intent only.
  </action>
  <verify>
    <automated>cd /Users/pvelikov/StudioProjects/stream-chat-android && ./gradlew :stream-chat-android-client:compileTestKotlin -x lint 2>&1 | tail -20</automated>
  </verify>
  <done>File exists, compiles without error, contains 12 @Disabled @Test stubs. Both test files present and test compilation succeeds.</done>
</task>

</tasks>

<verification>
Both test files compile cleanly under `:stream-chat-android-client:compileTestKotlin`. Running the quick-run command from VALIDATION.md produces output showing 0 tests run (all disabled) rather than a "no tests found" or "unresolved reference" build failure.

Quick-run command: `cd /Users/pvelikov/StudioProjects/stream-chat-android && ./gradlew :stream-chat-android-client:test --tests "*.ChannelStateImplPreservationTest" --tests "*.MessageIsLocalOnlyTest" -x lint 2>&1 | tail -20`
</verification>

<success_criteria>
- `MessageIsLocalOnlyTest.kt` exists at the correct test path with 8 @Disabled stubs
- `ChannelStateImplPreservationTest.kt` exists at the correct test path with 12 @Disabled stubs
- `:stream-chat-android-client:compileTestKotlin` succeeds with no errors
- Wave 1 and Wave 2 plans can reference `--tests "*.MessageIsLocalOnlyTest"` and `--tests "*.ChannelStateImplPreservationTest"` without "no tests found" failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-reconciliation/01-01-SUMMARY.md`
</output>
